export const defaultStrokeColors = ['var(--foreground)'];
export const defaultFillColors = ['var(--primary)', 'var(--secondary)'];
export const defaultPieColors = [
  'var(--chart-1)',
  'var(--chart-2)',
  'var(--chart-3)',
  'var(--chart-4)',
  'var(--chart-5)',
];

export const defaultTooltipBg = 'var(--background)';
export const defaultTooltipBorder = 'var(--border)';
export const defaultGridColor = 'var(--muted)';

export const formatValue = String;
