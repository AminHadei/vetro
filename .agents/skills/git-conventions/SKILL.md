---
name: git-conventions
description: Use when about to run git commit, write a commit message, or advise the user on commit-message format for the vetro repo. Loads the repo's Conventional-Commits rules — required types, scope conventions, terse-subject and no-body rule, ban on AI attribution trailers, never-bypass-husky rule, and the changeset-with-the-commit rule.
---

# Git conventions for vetro

Full rules — types, scopes, branch naming, what Husky enforces, and the changeset-with-the-commit rule — live in [`docs/content/git-conventions.md`](../../../docs/content/git-conventions.md). Read it before composing a commit message.

That doc is the single source of truth: it ships with the developer-facing VitePress site so humans and agents follow the same rules.
